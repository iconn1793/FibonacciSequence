FibsNum() contains the method fib(n) which will return the nth number in Fibonacci's sequence. 

Running FibsNum's main method will prompt you to enter a value for n. Otherwise, calling the method recFib(n) will allow you to pass n in directly as an argument. 
